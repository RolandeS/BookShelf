<h5 class="notify"><span class="notifyTitle">Notifications</span> <span class="notifyAlt"><%= image_tag 'notify.png', class: 'notifyIcon' %></span></h5>
<% if current_user.books.count > 100 || current_user.books.where(read: 'Revisit').count > 5 %>
  <div class="arrow-up"></div>
  <p>
  <% if current_user.books.count > 100 %>
  "You have <%= current_user.books.count %> books, start deleting!"</br> 
  <% end %> 

  "You have <%= current_user.books.where(read: 'Revisit').count %> books to revisit."</br>
  <% book = current_user.books.where(last_clicked: nil).sample %> 
  <% if book != nil %>
    "<i>'<%= book.name.capitalize %>'</i> hasn't been read. Maybe you should check it out or delete it."</p>
  <% end %>
<% elsif current_user.books.where(last_clicked: nil).count > 10 %></br>
  <div class="arrow-up"></div>
  <p>"You have <%= current_user.books.where(last_clicked: nil).count %> unread books, start reading!"</p>
<% else %>
  <div class="arrow-up"></div>
  <p>"Your BookShelf looks awesome!"</p>
<% end %>

