<h5 class="notify">Notifications</h5>
<% if current_user.books.count > 50 %>
  <p>You have <%= current_user.books.count %> books, start deleting!</p>  
<% end %>
<% if current_user.books.where(read: 'Revisit').count > 5 %>
  You have <%= current_user.books.where(read: 'Revisit').count %> books to revisit.</br>

  <% book = current_user.books.where(last_clicked: nil).sample %> <%= book.name %> hasn't been read. Maybe you should check it out or delete it.</p>

<% elsif current_user.books.where(last_clicked: nil).count > 10 %></br>
  You have <%= current_user.books.where(last_clicked: nil).count %> unread books, start reading!</br>
<% else %>
  <p>Your BookShelf looks awesome!</p>
<% end %>

