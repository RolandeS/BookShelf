$('.delete_book').on('ajax:success', function() {  
  $(this).closest('div').remove();
});  

$("#shelf-<%= @shelf.id %>").replaceWith("<%= j(render @shelf)%>");
$(".notifications").html("<%= j(render 'notifications')%>");
carouselInit("#shelf-<%= @shelf.id %> .owl-carousel")
flipInit(".card");
